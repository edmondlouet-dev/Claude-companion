{
  "name": "poreless-proxy",
  "version": "1.0.0",
  "description": "Thin proxy server — keeps the INCI API key out of the mobile bundle.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18"
  }
}
